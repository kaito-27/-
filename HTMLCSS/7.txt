・<div>
<div>タグは、一塊の範囲として定義する際に使用する。特別な意味はない。
スタイル調整をする場合や、class,idなど属性をつけ、同じ値をまとめて指定するのに便利。

・<p>
<p>タグは、段落を指定するタグ。
<p>...</p>で囲んだテキストがひと段落である。

・<span>
<span>タグは、<span>...</span>で囲んだテキストをグループ化し、スタイル調整ができる。
<div>タグと似ているが、<span>タグはインライン要素なので、前後に改行が入らない。

ブロック要素は<div>と<p>